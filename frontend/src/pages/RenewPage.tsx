/**
 * RenewPage — stub for open-source builds.
 *
 * When the private payment submodule is present, this file is replaced
 * by a symlink to the real implementation via `make setup-payment`.
 */
export default function RenewPage() {
  return null;
}

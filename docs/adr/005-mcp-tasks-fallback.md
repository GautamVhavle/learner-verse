# ADR 005: Keep application job tools as the baseline

Status: accepted

The current MCP Tasks extension is not implemented by the selected Python SDK. Future MCP tools therefore return durable application job IDs and provide get/cancel/result operations. An opt-in Tasks adapter can be added after SDK and conformance support are available.

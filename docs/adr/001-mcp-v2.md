# ADR 001: Use MCP Python SDK v2 in the future adapter

Status: accepted

Future MCP integration will use the official Python SDK v2 and the latest supported MCP specification. Domain services remain transport-neutral; MCP is an adapter added in Batch C, not a dependency of this Batch A foundation.
